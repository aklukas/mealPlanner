{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "Your favorite meals by nutrient count in one place.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json"
  }
}
